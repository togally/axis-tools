interface:
  display_name: "axis-tools-skill-update"
  short_description: "Refresh or migrate local Axis skills / 刷新或迁移本地 Axis 技能"
  default_prompt: "Use $axis-tools-skill-update to inventory, validate, back up, refresh, or migrate selected local Axis packaged skills from axis-tools."

policy:
  allow_implicit_invocation: true
