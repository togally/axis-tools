interface:
  display_name: "axis-create-skill"
  short_description: "Scan reusable skill candidates and assign reasoning levels / 扫描可复用技能候选并指定推理等级"
  default_prompt: "Use $axis-create-skill to scan this conversation for public-safe reusable skill candidates."

policy:
  allow_implicit_invocation: true

model:
  reasoning_level: "high"
  task_difficulty: "complex"
