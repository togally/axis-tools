interface:
  display_name: "Axis Create Skill"
  short_description: "Scan for public-safe reusable Axis skill candidates"
  default_prompt: "Use $axis-create-skill to scan this conversation for public-safe reusable skill candidates."

policy:
  allow_implicit_invocation: true
