interface:
  display_name: "axis-create-skill"
  short_description: "Scan reusable skill candidates / 扫描可复用技能候选"
  default_prompt: "Use $axis-create-skill to scan this conversation for public-safe, cross-project Axis skill candidates and route Orbit-owned content workflows to $orbit-skill-creator."

policy:
  allow_implicit_invocation: true
