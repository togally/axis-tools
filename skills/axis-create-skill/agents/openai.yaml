interface:
  display_name: "axis-create-skill"
  short_description: "Scan reusable skill candidates / 扫描可复用技能候选"
  default_prompt: "Use $axis-create-skill to create or refactor one reusable Axis or Orbit skill, choose the correct repository and prefix, then validate it."

policy:
  allow_implicit_invocation: true
