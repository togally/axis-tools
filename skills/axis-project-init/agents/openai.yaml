interface:
  display_name: "Axis Project Init"
  short_description: "Configure v0.2 once / 一次确认完成 v0.2 配置"
  default_prompt: "Use $axis-project-init to inspect an Axis v0.2 project configuration, present one complete confirmation bundle, and apply only after one consolidated user confirmation."

policy:
  allow_implicit_invocation: true
