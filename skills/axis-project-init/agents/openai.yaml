interface:
  display_name: "axis-project-init"
  short_description: "Configure v0.2 projects conversationally / 对话式配置 v0.2 项目"
  default_prompt: "Use $axis-project-init to inspect, confirm, migrate, and apply Axis v0.2 project configuration for a target repository."

policy:
  allow_implicit_invocation: true
