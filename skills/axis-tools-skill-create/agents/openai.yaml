interface:
  display_name: "axis-tools-skill-create"
  short_description: "Create or refactor reusable skills / 创建或重构可复用技能"
  default_prompt: "Use $axis-tools-skill-create as the single Axis/Orbit skill-creation entrypoint; require $axis-tools-prompt-create evidence for non-trivial prompts, then validate and report the bundle."

policy:
  allow_implicit_invocation: true
