interface:
  display_name: "axis-code-capture"
  short_description: "Package completed coding evidence / 打包已完成编码证据"
  default_prompt: "Use $axis-code-capture to package existing public-safe implementation evidence as one Axis v0.2 coding capture without publishing it."

policy:
  allow_implicit_invocation: false
