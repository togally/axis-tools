interface:
  display_name: "axis-doc-development"
  short_description: "Create one feature's traceable document set / 创建单功能可追溯开发文档集"
  default_prompt: "Use $axis-doc-development to resolve one feature, create or revise its approved development-document set, and return a separate project-knowledge change set without editing canonical knowledge or uploading to OSS."

policy:
  allow_implicit_invocation: true
