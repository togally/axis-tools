interface:
  display_name: "API Benchmark"
  short_description: "Benchmark test APIs safely"
  default_prompt: "Use $axis-api-benchmark to safely benchmark test-environment APIs and explain capacity."

policy:
  allow_implicit_invocation: true
