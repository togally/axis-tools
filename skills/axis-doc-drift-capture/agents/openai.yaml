interface:
  display_name: "axis-doc-drift-capture"
  short_description: "Capture task records and document drift / 记录任务执行与文档漂移"
  default_prompt: "Use $axis-doc-drift-capture to create public-safe task and version records, classify affected canonical documents, and recommend follow-up without rewriting project knowledge."

policy:
  allow_implicit_invocation: true
