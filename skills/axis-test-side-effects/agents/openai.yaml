interface:
  display_name: "axis-test-side-effects"
  short_description: "Verify approved real side effects / 验证已授权真实副作用"
  default_prompt: "Use $axis-test-side-effects to obtain exact current-run approval, execute one bounded real action, observe its effect, and verify cleanup."

policy:
  allow_implicit_invocation: true
