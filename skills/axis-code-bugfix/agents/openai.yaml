interface:
  display_name: "axis-code-bugfix"
  short_description: "Diagnose and fix from evidence / 基于证据诊断并修复缺陷"
  default_prompt: "Use $axis-code-bugfix to prove the reported failure's root cause and implement a regression-backed fix only when authorized."

policy:
  allow_implicit_invocation: true
