interface:
  display_name: "axis-test-report"
  short_description: "Capture v0.2 validation reports / 采集 v0.2 测试报告"
  default_prompt: "Use $axis-test-report to write a public-safe Axis v0.2 test or benchmark report package."

policy:
  allow_implicit_invocation: true
