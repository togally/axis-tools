interface:
  display_name: "axis-test-report"
  short_description: "Capture validation reports / 采集测试验证报告"
  default_prompt: "Use $axis-test-report to write a public-safe Axis test or benchmark report package."

policy:
  allow_implicit_invocation: true
