interface:
  display_name: "axis-doc-project-init"
  short_description: "Inspect and confirm Axis v0.2 configuration / 检查并一次确认 Axis v0.2 配置"
  default_prompt: "Use $axis-doc-project-init to inspect the target repository, present one public-safe Axis v0.2 confirmation bundle, and write only after one consolidated user approval."

policy:
  allow_implicit_invocation: true
