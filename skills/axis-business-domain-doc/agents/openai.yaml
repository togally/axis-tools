interface:
  display_name: "Axis Business Domain Doc"
  short_description: "Reconcile or design domain requirements / 梳理或设计业务域需求"
  default_prompt: "Use $axis-business-domain-doc in scan_and_reconcile or requirement_design mode, resolve one business_id without guessing, and update the reviewed business architecture summary."

policy:
  allow_implicit_invocation: true
