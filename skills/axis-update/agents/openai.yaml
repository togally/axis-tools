interface:
  display_name: "axis-update"
  short_description: "Refresh local Axis skills / 刷新本地 Axis 技能"
  default_prompt: "Use $axis-update to refresh local Axis packaged skills from axis-tools."

policy:
  allow_implicit_invocation: true
