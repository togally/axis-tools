interface:
  display_name: "axis-oss-publish"
  short_description: "Publish v0.2 outbox packages / 发布 v0.2 outbox 包"
  default_prompt: "Use $axis-oss-publish to validate and publish an Axis v0.2 local outbox package to the configured Aliyun OSS profile."

policy:
  allow_implicit_invocation: true
