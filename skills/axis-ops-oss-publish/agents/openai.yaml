interface:
  display_name: "axis-ops-oss-publish"
  short_description: "Validate or explicitly publish v0.2 packages / 校验或经确认发布 v0.2 包"
  default_prompt: "Use $axis-ops-oss-publish to validate or dry-run one Axis v0.2 outbox package, and upload only after approval for the exact run_id and target_prefix."

policy:
  allow_implicit_invocation: true
