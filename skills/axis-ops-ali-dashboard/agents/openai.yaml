interface:
  display_name: "axis-ops-ali-dashboard"
  short_description: "Create and validate Aliyun dashboard JSON / 创建并校验阿里云仪表盘 JSON"
  default_prompt: "Use $axis-ops-ali-dashboard to create, repair, and validate one Alibaba Cloud dashboard JSON artifact with evidence-backed SLS drilldowns."

policy:
  allow_implicit_invocation: true
